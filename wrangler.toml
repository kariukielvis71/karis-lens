name = "karis-media"
pages_build_output_dir = "."
compatibility_date = "2026-09-01"

[[d1_databases]]
binding = "DB"                       # -> env.DB in functions/api/[[path]].js
database_name = "karis-media-db"
database_id = "5681257f-cb85-4b2d-9313-a0d7e92dfd15"

# ADMIN_SETUP_SECRET is a secret, not a var — set it in the dashboard:
# Settings -> Environment variables -> add ADMIN_SETUP_SECRET as "Encrypted".